global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/rules/smartdeploy.yaml

scrape_configs:
  - job_name: smartdeploy-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8080
