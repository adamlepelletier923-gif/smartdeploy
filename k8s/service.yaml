apiVersion: v1
kind: Service
metadata:
  name: smartdeploy-api
  labels:
    app: smartdeploy-api
spec:
  type: ClusterIP
  selector:
    app: smartdeploy-api
  ports:
    - name: http
      port: 80
      targetPort: http
